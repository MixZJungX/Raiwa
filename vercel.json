{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    // ถ้าเป็น SPA แล้วอยากให้ทุกเส้นทางโหลด index.html
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
